// Admin pages import `AdminPageHeader`; it is the shared header under
// another name. Kept as an alias so the admin pages did not all need editing
// when the header moved out to be shared with the buyer console.
export { PageHeader as AdminPageHeader } from "@/components/page-header";
